# Tasks

1. Align build/package identity and outputs (`vite.config.ts`, `package.json`).
2. Update consumer-facing README usage and maintainer checklist.
3. Validate with lint/tests/build and `npm pack --dry-run`.
4. Confirm artifact paths and package file list.
